clincnvFolder: /root/programs/ClinCNV
ngsbitsFolder: /root/programs/NGSbits/ngs-bits/bin

# default values
scoreG: 20  # sensitivity and specificity balance
minimumNumOfElemsInCluster: 100  # minimum number of elements in cluster. Clustering happens only if nSamples > than 3 by number of elements in cluster
